import { component$ } from '@builder.io/qwik';

export interface SeaBattlePlayerGridProps {

}

export const SeaBattlePlayerGrid = component$<SeaBattlePlayerGridProps>((props) => {
  return (
    <div>
      SeaBattlePlayerGrid component works!
    </div>
  );
});
