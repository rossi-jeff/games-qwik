import { component$ } from '@builder.io/qwik';

export interface SeaBattleOpponentGridProps {

}

export const SeaBattleOpponentGrid = component$<SeaBattleOpponentGridProps>((props) => {
  return (
    <div>
      SeaBattleOpponentGrid component works!
    </div>
  );
});
